# Sample Output

The included sample values are fake. Run:

```bash
logsentinel scan samples/example.log --format json --fail-on never
```

Output includes status, timestamp, source, severity counts, and redacted findings.
